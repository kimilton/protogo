{
  "protoc": {
      "path": "${env.GOPATH}/bin/protoc.exe",
      "compile_on_save": false,
      "options": [
          "--proto_path=${workspaceRoot}/proto",
          "--proto_path=${env.GOPATH}/src",
          "--go_opt=paths=source_relative",
          "--go_out=./proto"
      ]
  }
}
